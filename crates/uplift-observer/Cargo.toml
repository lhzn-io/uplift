[package]
name = "uplift-observer"
version = "0.1.0"
edition = "2021"

[dependencies]
uplift = { path = "../uplift" }
zeroclaw-runtime = { path = "../../stack/zeroclaw/crates/zeroclaw-runtime", default-features = false }
async-trait = "0.1"
uuid = { version = "1.10", features = ["v4"] }
serde_json = "1.0"
chrono = "0.4"
